# Scripts

This directory contains optional automation scripts (Bash and PowerShell) to help manage the novel project files.

These scripts are not required for the core workflow, which is designed to run via AI agents or manual prompting. They are provided as utilities for power users who want to automate file creation and basic checks from the command line.

(Note: You can adapt these scripts to your specific directory structure if needed.)
